// Estrutura da tabela criadores
// CREATE TABLE criadores (id INT AUTO_INCREMENT PRIMARY KEY, usuario_id INT, descricao TEXT, preco_assinatura DECIMAL(10,2), ganhos_totais DECIMAL(10,2) DEFAULT 0);
