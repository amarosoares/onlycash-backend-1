// Estrutura da tabela conteudos
// CREATE TABLE conteudos (id INT AUTO_INCREMENT PRIMARY KEY, criador_id INT, tipo VARCHAR(20), titulo VARCHAR(255), preco DECIMAL(10,2), bloqueado BOOLEAN DEFAULT 1, data_upload TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
