// Estrutura da tabela usuários
// CREATE TABLE usuarios (id INT AUTO_INCREMENT PRIMARY KEY, nome VARCHAR(100), email VARCHAR(100), senha VARCHAR(255), tipo VARCHAR(20), verificado BOOLEAN DEFAULT 0, data_criacao TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
