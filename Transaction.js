// Estrutura da tabela transacoes
// CREATE TABLE transacoes (id INT AUTO_INCREMENT PRIMARY KEY, usuario_id INT, criador_id INT, valor DECIMAL(10,2), tipo VARCHAR(50), status VARCHAR(20), data TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
